version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - echo Compiling the Go code...
      - go build knights.go
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - knights
